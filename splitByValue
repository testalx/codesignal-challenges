std::vector<int> splitByValue(int k, std::vector<int> v) {
vector<int> a;
for(int i=0;i<v.size();i++)
    if(v[i]<k)
        a.push_back(v[i]);
for(int j=0;j<v.size();j++)
    if(v[j]>=k)
        a.push_back(v[j]);
return a;
}
