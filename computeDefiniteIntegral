double F,computeDefiniteIntegral(int a, int b, auto p) {
for(int i=0;i<size(p);)
    F+=1./(i+1)*p[i]*(pow(b,i+1)-pow(a,++i));
//cout<<"F(b)="<<F;
//for(int i=0;i<p.size();i++)
//F-=1./(i+1)*p[i]*pow(a,i+1);

return F;
}
